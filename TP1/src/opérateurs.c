#include <stdio.h>

int main(void)
{

int a = 16
int b = 3

int somme(a, b) {
  somme = a + b;
return somme;

int soustraction(a, b) {
  soustraction = b - a;
return soustraction;

int multiplication(a, b) {
  multiplication = a * b;
return multiplication;

int division(a, b) {
  multiplication = a / b;
return division;

int modulo(a, b) {
  modulo = a%b;
return modulo;
